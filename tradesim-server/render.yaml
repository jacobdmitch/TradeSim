# NOTE: Render only reads a Blueprint named render.yaml from the REPOSITORY ROOT.
# The active blueprint now lives at ../render.yaml (the TradeSim repo root) and
# points back here via `rootDir: tradesim-server`.
#
# This file is kept only as a reference copy and is NOT used by Render. Edit the
# root ../render.yaml instead.
